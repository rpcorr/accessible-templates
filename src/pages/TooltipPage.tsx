import { TooltipExamples } from '../examples/Tooltip/TooltipExamples';

export function TooltipPage() {
  return (
    <div className="page">
      <h2>Tooltip</h2>

      <p>
        An accessible tooltip component that provides additional information
        when a control receives focus or is hovered.
      </p>

      <TooltipExamples />
    </div>
  );
}
